<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ai.mapper.UserMapper">

    <!-- 通用查询列（适配UserDTO） -->
    <sql id="Base_Column_List">
        id, name, password, email, phone, role, status,
        created_at as createdAt, updated_at as updatedAt,
        last_login_time as lastLoginTime, gender, avatar, bio
    </sql>

    <!-- 根据用户名统计 -->
    <select id="countByName" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM user WHERE name = #{name}
    </select>

    <!-- 根据邮箱统计 -->
    <select id="countByEmail" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM user WHERE email = #{email}
    </select>

    <!-- 根据手机号统计 -->
    <select id="countByPhone" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM user WHERE phone = #{phone}
    </select>

    <!-- 插入用户 -->
    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (
            name, password, email, phone, role, status,
            created_at, updated_at, last_login_time, gender, avatar, bio
        ) VALUES (
                     #{name}, #{password}, #{email}, #{phone}, #{role}, #{status},
                     #{createdAt}, #{updatedAt}, #{lastLoginTime}, #{gender}, #{avatar}, #{bio}
                 )
    </insert>

    <!-- 根据用户名查询 -->
    <select id="selectUserByName" resultType="com.ai.dto.UserDTO">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE name = #{name}
    </select>

    <!-- 方法1：登录专用 - 仅更新登录时间和更新时间（替代原来的 updateUserById） -->
    <update id="updateLoginTime">
        UPDATE user
        SET last_login_time = #{lastLoginTime},
        updated_at = NOW() <!-- 直接用数据库时间，更准确 -->
        WHERE id = #{id}
    </update>

    <!-- 根据ID查询 -->
    <select id="getUserById" resultType="com.ai.dto.UserDTO">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE id = #{userId}
    </select>


    <!-- 方法2：修改资料专用 - 动态 SQL，只更新前端传入的非 null 字段 -->
    <update id="updateUserProfile">
        UPDATE user
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="bio != null">bio = #{bio},</if>
            updated_at = NOW() <!-- 无论改啥，都自动更新更新时间 -->
        </set>
        WHERE id = #{id}
    </update>

    <!--  修改密码  -->
    <update id="updatePassword">
        UPDATE user
        SET password = #{encodedNewPassword}
        WHERE id = #{userId}
    </update>

    <select id="selectNameById" resultType="java.lang.String">
        select name
        from user
        where id = #{userId}
    </select>



</mapper>