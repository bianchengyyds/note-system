<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ai.mapper.LogMapper">

    <select id="list" resultType="com.ai.pojo.LogInfo">
        SELECT id, operation, target_type, target_id, ip, user_agent, created_at
        FROM operation_log
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>


</mapper>