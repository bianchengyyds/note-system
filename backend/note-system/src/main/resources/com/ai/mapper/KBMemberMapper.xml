<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ai.mapper.KBMemberMapper">

    <!-- 根据知识库ID和用户ID查询成员 -->
    <select id="selectByKbIdAndUserId" resultType="com.ai.dto.KBMemberDTO">
        SELECT kb_id AS kbId, user_id AS userId, role, joined_at AS joinedAt
        FROM kb_member
        WHERE kb_id = #{kbId} AND user_id = #{userId} AND deleted = 0
    </select>
    <!-- 根据知识库ID和用户ID查询成员, 包括被删除的 -->
    <select id="selectByKbIdAndUserIdIncludeDeleted" resultType="com.ai.dto.KBMemberDTO">
        SELECT kb_id AS kbId, user_id AS userId, role, joined_at AS joinedAt, deleted
        FROM kb_member
        WHERE kb_id = #{kbId} AND user_id = #{userId}
    </select>

    <!-- 新增成员 -->
    <insert id="insert" parameterType="com.ai.dto.KBMemberDTO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO kb_member (kb_id, user_id, role, joined_at, deleted)
        VALUES (#{kbId}, #{userId}, #{role}, #{joinedAt}, 0)
    </insert>

    <!--  删除成员  -->
    <update id="updateDeletedByKbIdAndUserId">
        UPDATE kb_member
        SET deleted = #{deleted}
        WHERE kb_id = #{kbId} AND user_id = #{userId}
    </update>

    <!-- 查询用户在知识库中的角色 -->
    <select id="selectRoleByKbIdAndUserId" resultType="java.lang.String">
        SELECT role FROM kb_member
        WHERE kb_id = #{kbId} AND user_id = #{userId} AND deleted = 0
    </select>

    <!-- 批量删除成员 -->
    <update id="updateDeletedByKbId">
        UPDATE kb_member
        SET deleted = #{deleted}
        WHERE kb_id = #{kbId}
    </update>

    <!-- 物理删除指定知识库的所有成员 -->
    <delete id="deleteByKbIdPermanently">
        DELETE FROM kb_member
        WHERE kb_id = #{kbId}
    </delete>

    <!-- 修改成员角色 -->
    <update id="updateRoleByKbIdAndUserId">
        UPDATE kb_member
        SET role = #{role}
        WHERE kb_id = #{kbId} AND user_id = #{userId}
    </update>


    <!-- 根据知识库ID查询所有成员 -->
    <select id="selectListByKbId" resultType="com.ai.dto.KBMemberDTO">
        SELECT kb_id, user_id, role, joined_at
        FROM kb_member
        WHERE kb_id = #{kbId} AND deleted = 0
    </select>
</mapper>