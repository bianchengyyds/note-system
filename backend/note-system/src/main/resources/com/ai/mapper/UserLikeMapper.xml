<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ai.mapper.UserLikeMapper">
    <select id="selectByUserAndTarget" resultType="com.ai.dto.UserLike">
        SELECT * FROM user_like
        WHERE user_id = #{userId}
          AND target_type = #{targetType}
          AND target_id = #{targetId}
    </select>

    <insert id="insert" parameterType="com.ai.dto.UserLike"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO user_like
        (user_id, target_type, target_id, created_at)
        VALUES
        (#{userId}, #{targetType}, #{targetId}, #{createdAt})
    </insert>

    <delete id="deleteByUserAndTarget">
        DELETE FROM user_like
        WHERE user_id = #{userId}
          AND target_type = #{targetType}
          AND target_id = #{targetId}
    </delete>
</mapper>